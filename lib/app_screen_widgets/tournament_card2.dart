import 'package:flutter/material.dart';

class TournamentCardNormal extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      elevation: 5.0,
      margin: EdgeInsets.all(5.0),
      child: Container(),
    );
  }
}
